all:
  children:
    webservers:
      hosts:
        srv-web01:
          ansible_host: "IP du serveur"
          ansible_user: "wilder"
    controlnode:
      hosts:
        controlnode:
          ansible_connection: local
